# 🛠️ Edge-TTS Desktop — Документация для разработчиков

**Техническая шпаргалка для разработчиков и ИИ-агентов**

Этот документ содержит краткую справку о структуре проекта, ключевых файлах и местах, где нужно вносить изменения. Идеально подходит для быстрого погружения в кодовую базу.

---

## 📑 Оглавление

- [Требования к окружению](#-требования-к-окружению)
- [Основные файлы и директории](#-основные-файлы-и-директории)
- [Сборка](#-сборка)
- [Настройки и данные](#-настройки-и-данные)
- [Ключевые элементы поведения](#-ключевые-элементы-поведения)
- [Пакетная обработка](#-пакетная-обработка)
- [Игнорируемые файлы](#-игнорируемые-файлы)
- [Где вносить изменения](#-где-вносить-изменения)
- [Сторонние библиотеки](#-сторонние-библиотеки)
- [Gemini API интеграция](#-gemini-api-интеграция)
- [Зависимости](#-зависимости)
- [Полезные заметки](#-полезные-заметки)

---

## 💻 Требования к окружению

- **Python:** 3.10 или выше
- **FFmpeg:** Для склейки длинных аудиофайлов (опционально, но рекомендуется)
- **Зависимости:** Установите через `pip install -r requirements.txt`

---

## 📁 Основные файлы и директории

### Точка входа и ресурсы

- [`main.py`](main.py) — точка входа GUI приложения
- [`logo.ico`](logo.ico) — иконка приложения

### Модули приложения (`app/`)

| Файл | Назначение |
|------|------------|
| [`__init__.py`](app/__init__.py) | Инициализация пакета |
| [`main_window.py`](app/main_window.py) | Главный GUI (PySide6), элементы управления VLESS, логи, поле Gemini API, кнопка словаря замен |
| [`tts_worker.py`](app/tts_worker.py) | QThread для генерации TTS (использует `edge_tts` + `ssml_client`) |
| [`ssml_client.py`](app/ssml_client.py) | Кастомный SSML клиент для обхода экранирования `edge-tts` |
| [`config.py`](app/config.py) | Загрузка конфигурации из `.env` |
| [`logger.py`](app/logger.py) | Логгер в файл `logs/edge_tts_app.log` |
| [`version.py`](app/version.py) | Управление версией: константа `FROZEN_VERSION` для .exe, чтение из `VERSION` для разработки |
| [`voices.py`](app/voices.py) | Список доступных голосов |
| [`custom_dictionary.py`](app/custom_dictionary.py) | Пользовательский словарь замен для коррекции произношения |
| [`yo_processor.py`](app/yo_processor.py) | Обертка над библиотекой `yoditor` для ё-фикации |
| [`gemini_client.py`](app/gemini_client.py) | Клиент Gemini API для контекстной обработки |
| [`yo_gemini_async.py`](app/yo_gemini_async.py) | Асинхронная ё-фикация через Gemini 2.5 Flash |
| [`text_pipeline.py`](app/text_pipeline.py) | Гибридный конвейер обработки текста (Словарь + Yoditor + Gemini) |
| [`edge_tts_patch.py`](app/edge_tts_patch.py) | Monkey-patch для `edge-tts` (поддержка raw SSML) |
| [`srt_parser.py`](app/srt_parser.py) | Парсер .srt файлов |
| [`voice_markers.py`](app/voice_markers.py) | Управление метками голосов для субтитров |
| [`srt_audio_generator.py`](app/srt_audio_generator.py) | Генератор аудио из субтитров |
| [`ipa_helper.py`](app/ipa_helper.py) | Взаимодействие с Gemini для получения IPA транскрипций |
| [`gemini_corrector.py`](app/gemini_corrector.py) | Унифицированный модуль коррекции (ё-фикация + ударения) |
| [`gemini_stats.py`](app/gemini_stats.py) | Модуль сбора и хранения детальной статистики использования Gemini |
| [`gemini_triggers.py`](app/gemini_triggers.py) | Управление триггерными словами для контекстного анализа |

### Корневые модули

- [`vless_manager.py`](vless_manager.py) — VLESS клиент-обертка для xray-core / v2ray-core

---

## 🔨 Сборка

### Инструменты сборки (`Build_Tools/`)

| Файл | Назначение |
|------|------------|
| [`Edge_TTS_Desktop.spec`](Build_Tools/Edge_TTS_Desktop.spec) | Спецификация для PyInstaller |
| [`PyCompiler_settings.json`](Build_Tools/PyCompiler_settings.json) | Настройки компилятора |
| [`post_build.py`](Build_Tools/post_build.py) | Копирование VERSION/.env.example, очистка временных файлов |
| [`SpecCompiler.pyw`](Build_Tools/SpecCompiler.pyw) | GUI для запуска PyInstaller со спецификацией |
| [`logo.ico`](Build_Tools/logo.ico) | Копия иконки для сборки |

### Вспомогательные скрипты (игнорируются в Git)

- `00_CrRel.pyw` — создание релизного архива
- `00_Move.pyw` — деплой (перемещение билда в целевую папку)

### Бинарные зависимости

- `xray.exe` — бинарный файл для VLESS (должен находиться рядом с приложением)

---

## ⚙️ Настройки и данные

### Конфигурационные файлы

| Файл | Описание |
|------|----------|
| [`.env.example`](.env.example) → `.env` | "Заводские" настройки по умолчанию. В релизе (.exe) находится в папке `_internal` |
| `edge_tts_settings.json` | Локальный файл настроек (приоритет над `.env`). Содержит: голос, скорость, геометрию окна, текст, паузы, VLESS URL, Gemini API key |
| [`VERSION`](VERSION) | Версия для разработки (не попадает в .exe) |
| [`RELEASE_NOTES.md`](RELEASE_NOTES.md) | История изменений и релизов |

### Версионирование

**Важно:** В проекте используется **двойная схема версионирования**:

1. **Для разработки (из исходников):**
   - Версия читается из файла [`VERSION`](VERSION)
   - Используется при запуске через `python main.py`

2. **Для скомпилированного .exe:**
   - Версия "зашита" в константе `FROZEN_VERSION` в [`app/version.py`](app/version.py)
   - Файл `VERSION` **не копируется** в дистрибутив

### Кнопки управления

**Одиночная озвучка:**

- **Preview** — прослушать перед сохранением
- **Save** — сохранить в файл
- **Cancel** — отменить генерацию

**Воспроизведение:**

- **Start** — начать озвучку
- **Pause** — пауза/возобновление (toggle)
- **Stop** — остановить

### VLESS

- **Connect** — подключение к VLESS-прокси
- **Disconnect** — отключение
- Прокси автоматически прокидывается в TTS worker

### Gemini API

- Поле ввода **Gemini API Key** в интерфейсе
- Используется для контекстной ё-фикации
- Работает через VLESS-прокси при необходимости

### Качество аудио

Три режима вывода MP3:

- **Standard** (48kbps)
- **High** (96kbps)
- **Ultra** (192kbps)

---

## 📦 Пакетная обработка

### Возможности

- **Мультивыделение:** `Ctrl` для выбора отдельных файлов, `Shift` для диапазона
- **История путей:** Запоминается 5 последних использованных папок
- **Автоопределение кодировки:** UTF-8 / Windows-1251
- **Сохранение состояния:** Список файлов и история путей сохраняются между запусками

---

## 🚫 Игнорируемые файлы

### `.gitignore` исключает:

- `edge_tts_settings.json` — локальные настройки
- `logs/` — логи приложения
- `.env`, `.env.*` — конфигурация
- `build/`, `dist/`, `*.egg-info/` — артефакты сборки
- Тестовые файлы (`test_*.py`)
- Вспомогательные скрипты (`00_*.pyw`)
- Build артефакты (`Edge_TTS_Desktop/`)

> ✅ Удалённые/неиспользуемые файлы уже выброшены из репозитория

---

## 🎯 Где вносить изменения

### UI и интерфейс

- **Стили, кнопки, layout:** [`app/main_window.py`](app/main_window.py)

### Логика обработки

- **Генерация TTS, паузы:** [`app/tts_worker.py`](app/tts_worker.py) (включает retry-логику: 3 попытки при сбоях)
- **SSML обработка:** [`app/ssml_client.py`](app/ssml_client.py)

### Сборка и деплой

- **Конфигурация PyInstaller:** [`Build_Tools/Edge_TTS_Desktop.spec`](Build_Tools/Edge_TTS_Desktop.spec)
- **Пост-обработка:** [`Build_Tools/post_build.py`](Build_Tools/post_build.py)

### Сетевые возможности

- **VLESS-клиент:** [`vless_manager.py`](vless_manager.py)

### Обработка текста

#### Пользовательский словарь замен

- **Модуль:** [`app/custom_dictionary.py`](app/custom_dictionary.py)
- **Файл словаря:** `custom_dictionary.txt` (рядом с `edge_tts_settings.json`)
- **Формат:** `Исходное_слово=Целевое_слово`

**Возможности:**
- Регистронезависимый поиск с сохранением регистра
- Замена только целых слов (используя `\b` regex boundaries)
- Автоматическая сортировка по алфавиту при сохранении
- Перезагрузка перед каждой генерацией TTS

**UI управление:**
- Кнопка "📖 Словарь замен" в главном окне
- Открывает файл в системном редакторе

**Пример:**
```
Конечной=Конечъной
Жёсткий=Жосткий
Юнова=Юнава
```

#### Ё-фикация текста

- **Словарный подход:** [`app/yo_processor.py`](app/yo_processor.py) (обертка над `libs/yoditor.py`)
- **AI-подход:** [`app/yo_gemini_async.py`](app/yo_gemini_async.py) (Gemini 2.5 Flash)
- **Гибридный конвейер:** [`app/text_pipeline.py`](app/text_pipeline.py) (Yoditor + Gemini)

### Gemini интеграция

- **Клиент:** [`app/gemini_client.py`](app/gemini_client.py)
- **Асинхронная обработка:** [`app/yo_gemini_async.py`](app/yo_gemini_async.py)
- **Конвейер:** [`app/text_pipeline.py`](app/text_pipeline.py)

### Расстановка ударений

### Расстановка ударений

- **AI-подход:** Gemini 2.5 Flash (Thinking Mode)
- **IPA теги:** Используются стандартные SSML теги `<phoneme alphabet='ipa' ph='...'>`
- **Raw SSML:** Реализовано через патч [`app/edge_tts_patch.py`](app/edge_tts_patch.py), так как стандартный `edge-tts` экранирует символы ударения.

### Озвучка субтитров

- **Парсинг:** [`app/srt_parser.py`](app/srt_parser.py)
- **Метки:** [`app/voice_markers.py`](app/voice_markers.py) (маппинг `[RU_M]` -> `ru-RU-DmitryNeural` и т.д.)
- **Генерация:** [`app/srt_audio_generator.py`](app/srt_audio_generator.py) (расчет пауз, склейка фрагментов)

---

## 📚 Сторонние библиотеки (Vendored)

### `libs/yoditor`

**Назначение:** Библиотека для восстановления буквы «ё» в русском тексте

**Почему vendored:** Включена в репозиторий для упрощения установки и обеспечения стабильности

**Использование:** Обрабатывает однозначные случаи ё-фикации на основе словаря

---

## 🤖 Gemini API интеграция

### Технические детали

- **Модель:** Gemini 2.5 Flash
- **Назначение:** Контекстная ё-фикация для неоднозначных случаев

### Гибридный подход

1. **Yoditor (словарь)** — быстрая обработка однозначных случаев
2. **Gemini AI (контекст)** — умная обработка неоднозначностей (все/всё, еще/ещё)

### Конфигурация

- **API Key:** Хранится в `edge_tts_settings.json`, вводится через GUI
- **Прокси:** Поддерживает работу через VLESS-прокси

### Оптимизация

Gemini вызывается только при обнаружении подозрительных слов через regex паттерн в [`text_pipeline.py`](app/text_pipeline.py)

---

## 📦 Зависимости (requirements.txt)

| Пакет | Версия | Назначение |
|-------|--------|------------|
| `edge-tts` | >=6.1.0 | Основная библиотека TTS |
| `aiohttp_socks` | >=0.8.4 | Поддержка SOCKS прокси |
| `PySide6` | latest | GUI фреймворк |
| `psutil` | >=6.0.0 | Системная информация |
| `python-dotenv` | latest | Загрузка `.env` файлов |
| `vless` | latest | VLESS протокол |
| `google-genai` | >=1.0.1 | Gemini API клиент |

---

## 💡 Полезные заметки

### Сохранение состояния

GUI автоматически сохраняет в `edge_tts_settings.json`:

- Геометрию и позицию окна
- Выбранный голос и скорость
- Состояние разделителя (splitter)
- Последний введенный текст
- Настройки пауз и процент укорачивания
- Gemini API key
- VLESS URL

### Режимы работы

Приложение поддерживает:

- **Одиночная озвучка** — вкладка "Один файл"
- **Пакетная обработка** — вкладка "Пакетная обработка"

### VLESS авто-подключение

VLESS может автоматически подключаться при старте, если:

- В `.env` установлено `VLESS_ENABLED=true`
- Указан `VLESS_URL`

### FFmpeg

Используется для склейки длинных аудиофайлов (>10,000 символов). Если FFmpeg не установлен, генерация длинных текстов будет недоступна.

---

**[⬆️ Вернуться к оглавлению](#-оглавление)**

> 📖 Для пользовательской документации см. [README.md](README.md)  
> 📝 Для истории изменений см. [RELEASE_NOTES.md](RELEASE_NOTES.md)
